import streamlit as st

# 1. กำหนดค่าคงที่
car1 = 4   # รถน้ำมัน 4 บาท/กม.
car2 = 1   # รถไฟฟ้า 1 บาท/กม.

# 2. ฟังก์ชันหลักของแอป Streamlit
def main():

    st.title("โปรแกรมคำนวณค่าใช้จ่ายการเดินทาง 🚗⚡")

    st.write(f"""
    ### อัตราค่าใช้จ่าย
    - รถยนต์ใช้น้ำมัน: **{car1} บาท/กม.**
    - รถไฟฟ้า: **{car2} บาท/กม.**
    """)

    # รับค่าระยะทาง
    kg = st.number_input("ระยะทางที่ต้องการเดินทาง (กิโลเมตร)", min_value=0.0, step=0.1)

    # เลือกประเภทยานพาหนะ
    car = st.radio("เลือกยานพาหนะ", 
                   options=[1, 2],
                   format_func=lambda x: "รถน้ำมัน (4 บาท/กม.)" if x == 1 else "รถไฟฟ้า (1 บาท/กม.)")

    # ปุ่มคำนวณ
    if st.button("คำนวณค่าใช้จ่าย"):
        if car == 1:
            total = kg * car1
            st.success(f"""
                ### ผลการคำนวณ
                - ระยะทางรวม: **{kg} กม.**
                - ยานพาหนะ: **รถน้ำมัน**
                - อัตราค่าใช้จ่าย: **{car1} บาท/กม.**
                - 💰 **ค่าใช้จ่ายรวม: {total} บาท**
            """)

        elif car == 2:
            total = kg * car2
            st.success(f"""
                ### ผลการคำนวณ
                - ระยะทางรวม: **{kg} กม.**
                - ยานพาหนะ: **รถไฟฟ้า**
                - อัตราค่าใช้จ่าย: **{car2} บาท/กม.**
                - 💰 **ค่าใช้จ่ายรวม: {total} บาท**
            """)

if __name__ == "__main__":
    main()
